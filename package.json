{
  "name": "npm-run-cli",
  "version": "1.0.0",
  "description": "CLI to run scripts in package.json",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "m@smuco.de",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^1.19.1"
  },
  "dependencies": {
    "inquirer": "^7.0.6",
    "ramda": "^0.27.0"
  },
  "bin": {
    "npm-run": "cli.js"
  },
  "engines": {
    "node": ">=8"
  }
}
